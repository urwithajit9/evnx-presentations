$ cat .evnx.toml
[[scan.patterns]]
name = "Acme internal token"
regex = "ACME-[A-Z0-9]{32}"
severity = "high"

$ evnx scan app.py
  config    .evnx.toml
! /tmp/claude-1000/-home-ajit-Pictures-ajit-evnx-cloud/9cc5da3a-53b9-46f4-b733-898b0033a185/scratchpad/persona-verify/pat/.evnx.toml: unknown key `scan.patterns[0].severity` (ignored)

  evnx scan  Checking for exposed secrets

  scanning app.py

  ✗  Acme internal token                         high
     app.py:1  ACME…Q5R6
     !  matches a live key format, not a placeholder

  1 finding  ·  1 high


Next steps:
  1. Revoke or rotate the keys above — assume they are compromised
  2. Remove them from history: git filter-repo --path .env --invert-paths
  3. Force push, after coordinating with everyone who has a clone
  Docs: https://www.evnx.dev/guides/commands/scan
exit=1
