$ evnx validate --strict

  evnx validate  Check environment configuration

  ✗  APP_KEY looks like a placeholder
     .env
     ·  fixable with --fix

  ✗  DB_PASSWORD looks like a placeholder
     .env
     ·  fixable with --fix

  ✗  APP_KEY is too weak or predictable
     .env
     →  Run: openssl rand -hex 32
     ·  fixable with --fix

  ✗  DB_PASSWORD is too weak or predictable
     .env
     →  Run: openssl rand -hex 32
     ·  fixable with --fix

  ✗  MAIL_PASSWORD is too weak or predictable
     .env
     →  Run: openssl rand -hex 32
     ·  fixable with --fix

  5 errors

Next steps:
  1. Review and fix the errors above
  2. Run with --fix to auto-correct common issues
  3. Use --ignore issue_type to suppress specific warnings
Validation failed: 5 errors
  Docs: https://www.evnx.dev/guides/commands/validate
exit=1
