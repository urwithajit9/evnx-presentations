$ mkdir shop && cd shop && git init -q
$ cat > package.json   # a Next.js app
$ evnx init --from-source

  evnx init  Set up environment variables for your project


  scanned 1 source file

  3 variables read by the code
     DATABASE_URL                 src/checkout.ts:2
     NEXT_PUBLIC_SITE_URL         src/checkout.ts:3
     STRIPE_SECRET_KEY            src/checkout.ts:1

  0 already in .env.example  ·  3 new
Error: found 3 variables the code reads that .env.example does not declare, but there is nobody to confirm with.

Accept them:  evnx init --from-source --yes

No verdict: evnx did not finish. This is not a clean result.

$ evnx init --from-source --yes

  evnx init  Set up environment variables for your project


  scanned 1 source file

  3 variables read by the code
     DATABASE_URL                 src/checkout.ts:2
     NEXT_PUBLIC_SITE_URL         src/checkout.ts:3
     STRIPE_SECRET_KEY            src/checkout.ts:1

  0 already in .env.example  ·  3 new
✓ Created .env from template
✓ Created .gitignore with .env*, !.env.example, !.env.sample, !.env.template
✓ Added 3 variables from the source

$ cat .env.example
# ── Database ──
# (required)
DATABASE_URL=https://example.com


# ── General ──
# read at src/checkout.ts:3
NEXT_PUBLIC_SITE_URL=


# ── Payments ──
# Secret key (server-side only)
STRIPE_SECRET_KEY=your_stripe_secret_key_value

# Generated by evnx v0.5.0 on 2026-09-25 03:11
